import QtQuick
import qs
import qs.settings

Text {
    color: Colors.onSurfaceColor
    font.family: "Material Symbols Rounded"
    font.pixelSize: 14
    font.variableAxes: ({
            GRAD: 200,
            wght: 400
        })
}
